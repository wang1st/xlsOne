# xlsOne 产品需求文档

## 产品概述

xlsOne 是一款 macOS 平台的 Excel 财务报表汇总工具，用于将多个格式相同的 Excel 文件合并成一个汇总表。

## 核心需求

### 1. 文件导入
- 支持拖拽导入多个 `.xlsx` 或 `.xls` 文件
- 支持通过菜单"打开文件"导入
- 显示已导入文件列表

### 2. 工作表(Sheet)管理
- 自动识别所有文件中的工作表名称
- 支持切换不同工作表进行查看和汇总
- 当切换工作表时，重新汇总该工作表的数据

### 3. 单元格聚合逻辑

#### 3.1 可聚合性判断
对于每个单元格位置（如 B2、D2），跨所有文件判断：

| 场景 | 判断结果 | 显示方式 |
|------|---------|---------|
| 所有文件的值相同 | 标签型编码，不可聚合 | 显示该值（如"201"） |
| 值不同且都是数字 | 金额，可聚合 | 显示求和结果（如"4500"） |
| 值不同且包含非数字 | 混合类型，不可聚合 | 显示"X条"（如"3条"） |
| 只有一个文件 | 直接显示值 | 显示该值 |

#### 3.2 数值解析规则
- 支持纯数字（如"1000"）
- 支持千分位格式（如"1,000.50"）
- 支持欧式格式（如"1.234,56"）

### 4. 界面需求

#### 4.1 主窗口布局
```
┌─────────────────────────────────────────┐
│ [打开] [导出]    xlsOne          [设置] │
├─────────────────────────────────────────┤
│ 工作表: [Sheet1] [Sheet2] [Sheet3]      │
├─────────────────────────────────────────┤
│     │  A   │  B   │  C   │  D   │      │
│  1  │ 科目 │ 代码 │ 名称 │ 金额 │      │
│  2  │  1   │ 201  │  xx  │ 4500 │      │  ← 金额聚合显示
│  3  │  2   │ 301  │  yy  │ 3200 │      │
└─────────────────────────────────────────┘
```

#### 4.2 单元格显示
- 可聚合的数值单元格：显示求和结果
- 不可聚合的标签单元格：显示原始值
- 混合类型单元格：显示"X条"表示有X个不同值

#### 4.3 穿透查阅
- 点击单元格可查看该位置在所有文件中的原始值
- 显示每个文件的文件名和对应值

### 5. 菜单需求

#### 5.1 应用菜单
- **文件**
  - 打开文件... (⌘O)
  - 添加文件... (⌘⇧O)
  - 关闭全部 (⌘⇧W)
- **编辑**
  - 重载 (⌘R)
  - 重置 (⌘T)

### 6. 导出功能
- 将汇总结果导出为新的 Excel 文件
- 保留原始格式（表头、行列结构）

## 技术架构

### 核心模块

```
xlsOneCore/
├── Models.swift           # 数据模型
├── ExcelParser.swift      # Excel 解析
├── SimpleMerger.swift     # 合并引擎（核心逻辑）
└── TypeRecognition/       # 类型识别（可选增强）

xlsOne/
├── XlsOneApp.swift        # 应用入口
├── ContentView.swift      # 主界面
└── Views/                 # 子视图组件
```

### 合并引擎逻辑

```swift
// 简化的两阶段处理
func merge(dataSets: [[[CellData]]]) -> [[MergedCell]] {
    for each cell position {
        // 阶段1: 判断是否可聚合
        let isAggregatable = checkIfAllValuesAreNumericAndDifferent()
        
        // 阶段2: 执行聚合或保留原值
        if isAggregatable {
            result = sum(all numeric values)
        } else {
            result = first value (as label)
        }
    }
}
```

## 测试场景

### 场景1: 仙居县财务报表
- 5个乡镇的决算报表
- B列：科目代码（如"201"、"301"）→ 不可聚合，显示原值
- D列：金额（如"1000"、"2000"）→ 可聚合，显示总和

### 场景2: 不同科目代码
- 文件1的B2="201"
- 文件2的B2="301"
- 结果：显示"编码 2条"（因为值不同且都是编码）

### 场景3: 金额汇总
- 文件1的D2="1000"
- 文件2的D2="2000"
- 文件3的D2="1500"
- 结果：显示"4500"

## 非功能性需求

### 性能
- 支持同时处理 20+ 个文件
- 单个文件不超过 1000 行
- 汇总操作在 2 秒内完成

### 兼容性
- macOS 12.0+
- 支持 .xlsx 和 .xls 格式

### 可靠性
- 解析失败时显示错误信息
- 部分文件失败不影响其他文件处理

## 待办事项

- [ ] 完善导出功能（当前仅显示"导出功能开发中"）
- [ ] 添加 LLM 类型识别（可选增强）
- [ ] 支持更多数字格式（如括号表示负数）
- [ ] 添加单元格格式保留（字体、颜色等）
