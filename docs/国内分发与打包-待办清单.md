# 国内 macOS / Windows 分发与打包 —— 待办清单

> 审查时间：2026-07-09 ｜ 最近更新：2026-07-10（仅刷新 Windows 国内段：后端部署 / 密钥安全已标记完成，补充构建与签名现状）
> 范围：中国大陆（品牌「表表归一 / z-pulse.cn」）macOS 与 Windows 的**分发**与**打包**。
> 结论先行：客户端与后端的「双口味（region flavor）」代码骨架已完成；**国内激活后端已部署上线**，剩余闭环动作集中在**代码签名、实际上传托管、下载页放开、支付联调**四类。

---

## 一、已经具备的基础（无需从零做）

| 模块 | 状态 | 证据 |
|------|------|------|
| Windows 客户端「国内口味」机制 | ✅ | `cpp/CMakePresets.json` 的 `windows-cn-release` 预设已把 `XLSONE_ACTIVATION_BASE_URL` 设为 `https://api.z-pulse.cn`；`cpp/scripts/package_windows_full.ps1 -Domestic` 会把它烤进二进制 |
| macOS 客户端「国内口味」机制 | ✅ | `Sources/xlsOneLicense/LicenseManager.swift` 读 Info.plist 键 `XLSONEActivationBaseURL`，国内= `api.z-pulse.cn`，并带跨区回退；App Store 渠道 `isAppStoreDistribution` 自动跳过激活 |
| Windows 安装包（MSI/ZIP） | ✅ | WiX + CPack；`package_windows_full.ps1` 产出已签名前的 MSI 与便携 ZIP（Qt 6.11.1 / MinGW 13.1） |
| macOS DMG 打包 + 签名 + 公证 | ✅ | `scripts/package_macos_dmg.sh` 支持 `--signed --team-id --notarize` |
| 国内激活后端代码 | ✅ | `activation/domestic-server/server.js` 路由齐全：`/api/activate/windows`、`/api/license/download`、`/api/webhook/afdian`、`/api/admin/generate*`、mac 端 `/api/activate|verify|refresh` |
| 离线激活页 / 下载页 | ✅ | `activation/domestic-server/public/offline.html`、`downloads.html` 已存在 |
| 部署脚本 | ✅ | `deploy/install.sh`、`nginx-api.z-pulse.cn.conf`、`xlsone-activation.service`、`upload_installer.py`、`.env.example` |
| 站点与备案 | ✅ | `site/` 已是真实站（含 ICP `浙ICP备20024036号-3` 与公安备案），首页/产品页/下载页/隐私页齐全 |
| EULA / 软著材料 | ✅ | `cpp/packaging/windows/licenses/EULA_zh_CN.txt`、`软件著作权申请资料/` |

---

## 二、Windows 国内 —— 还差什么

- [x] **部署国内激活后端（硬阻断 · 已完成）**
  - ✅ 后端已上线：`47.97.115.235` 跑 Node 服务，`/opt/xlsone-activation/.env` 已注入新 Ed25519 私钥种子 + `ACTIVATION_SECRET`（2026-07-09 轮换，与客户端 `license_manager.cpp` 公钥 `acc209…` 同源闭环）。
  - ✅ `api.z-pulse.cn` 已签 Let's Encrypt 证书（2026-07-09 → 2026-10-07），Nginx 反代 127.0.0.1:8787，80→443 重定向；`systemd` 服务存活；`https://api.z-pulse.cn/api/health` 公网返回 `{"status":"ok"}`。
  - ✅ 已补 `certbot-renew` 定时器自动续期；`activation/README.md` 架构漂移已修正（不再写「阿里云 FC (TBD)」，改为已上线的 Node 服务）。
  - 备注：双站（国内 + 国际 Worker）端到端授权链路 2026-07-09 已实测 PASS。
- [ ] **代码签名证书（暂缓 · 等软件盈利后再采购）**
  - ⏸️ **决策（2026-07-10）**：当前**不采购、不签名**，先以**未签名**方式分发（"爱装不装"）。已知代价：Windows SmartScreen 会弹「未知发布者 / Windows 已保护你的电脑」警告，用户需点「仍要运行」才能装/运行；接受此体验。
  - 打包脚本 `package_windows_full.ps1` 已支持 `-Sign`（Authenticode SHA256 + RFC3161 时间戳，PFX `-CertFile` 或证书存储 `-CertSha1`）；盈利后采购 **EV 证书**（优先能开国内发票的代理：天威诚信/DigiCert、数安时代/GlobalSign），到手后本机装 Windows SDK 拿 `signtool` 重跑 `-Sign` 即可。
  - 待办（盈利后）：证书采购 → 令牌/云端签名接入 → 对 `xlsOneQt.exe` 与 `.msi` 签名 → 重新上传。
- [ ] **国内下载托管（包已构建，未上传）**
  - ✅ 国内版 ZIP 已构建成功：`cpp/build-windows-cn-release/xlsone-1.0.4-windows-amd64.zip`（Qt 6.11.1 / MinGW 13.1，68.3MB，含 exe + Qt6 DLL + MinGW 运行时 + 插件 + 翻译，可在纯净 Win 直接解压运行，激活指向 `api.z-pulse.cn`）。
  - ⚠️ **MSI 尚未生成**：本沙箱缺可用 Windows Installer 校验子系统（`light.exe` 报 `LGHT0217`），在**正常 Windows 开发机**用同一条 `cpack -G WIX` 即可出 MSI；用户本机 Qt6 工具链已齐，仅缺 signtool。
  - ⬜ 用 `deploy/upload_installer.py` 把**未签名** ZIP（及后续 MSI）上传到服务器 `downloads`（需 root 密码）；`site/api/version.json` 增加 `downloads.windows_amd64` 字段并指向国内地址；`download.html` 放开 Windows 真实链接（当前仍「即将推出」）。
- [ ] **下载页放开 Windows（仍未做）**
  - `site/products/xlsone/download.html` 目前 Windows 仍卡在「即将推出」，且 `version.json` 无 `windows_amd64` 字段；需换成真实链接并补下载校验（SHA256）说明。
- [ ] **支付 / 爱发电联调（仍未做）**
  - `deploy/.env.example` 中 `AFDIAN_*` 仍为空 → webhook 会返回 401；需填真实爱发电 `USER_ID/TOKEN/WEBHOOK_TOKEN`，联调「下单→webhook→自动发码」。
- [ ] **试用到期封锁验证（仍未做）**
  - 确认未激活 / 14 天试用过期时功能是否真受限（代码有 `Trial` 状态，需实测）。
- [x] **锁定发货构建为 Qt 6.11.1**
  - 已统一走 `windows-cn-release`（Qt 6.11.1 + MinGW 13.1 + 国内域名 `api.z-pulse.cn`）；旧的 `build-windows-qt5-release` 目录为过期缓存，可删除。
- [x] **密钥安全（重要 · 已完成）**
  - ✅ `activation/domestic-server/deploy/.env.example` 已改为纯占位符（无真实密钥），仅通过 `.env`（权限 600、gitignore）注入；
  - 备注：git 历史曾含旧真实密钥，但已于 2026-07-09 **彻底轮换**新种子 + 客户端内嵌公钥同步更新，旧密钥视为失效；`.secrets/`、`activation/worker/.secrets/`、`.wrangler/` 等已加 `.gitignore`。

---

## 三、macOS 国内 —— 还差什么

- [ ] **国内 macOS 构建口味（代码已支持，但无配置）**
  - `project.yml` 目前只管 App Store 口味（Bundle ID、Team 走环境变量），**没有**把 `XLSONEActivationBaseURL` 设为 `api.z-pulse.cn` 的国内 scheme/plist；
  - 需新增一个国内直接分发（Developer ID）构建：单独 Info.plist 设 `XLSONEActivationBaseURL=https://api.z-pulse.cn`，与 Windows 的 `windows-cn-release` 对应。
- [ ] **Apple Developer ID 证书 + 公证（notarization）**
  - 国内用户从网页直接下载 DMG 安装，必须走 **Developer ID Application** 签名 + **公证（notarytool）**，`package_macos_dmg.sh` 已支持但需要真实证书、`APPLE_ID`、`APP_SPECIFIC_PASSWORD`；
  - 未公证的 DMG 在 macOS Gatekeeper 下会被拦。
- [ ] **上传与下载页接入**
  - 把国内签名+公证后的 DMG 传到 `api.z-pulse.cn/downloads`（或 OSS/CDN）；
  - `version.json` 增加 `downloads.macos`（区分 arm64 / universal），下载页放开 macOS「即将推出」。
- [ ] **macOS 国内激活闭环联调**
  - 后端已支持 mac 端 `/api/activate`、`/api/verify`（HMAC/JWT），需用国内口味 mac 客户端与 `api.z-pulse.cn` 跑通「在线激活 / 离线导入 / 换机」。
- [ ] **分发渠道策略确认**
  - 明确：国内 macOS 走「App Store（国际，自动激活免授权）」还是「国内 Developer ID 直发（走 api.z-pulse.cn 授权）」，二者激活逻辑互斥，需选定并固化。

---

## 四、共同 / 合规

- [ ] 软件著作权登记正式提交（资料已生成于 `软件著作权申请资料/正式资料/`）。
- [ ] 用户协议 & 隐私政策（中文正式版）复核：`site/privacy/` 已在线，需确认是国内合规版本。
- [ ] 等保（如监管要求）。
- [ ] 反盗版（代码混淆）列入后期。
- [x] **版本号一致性（2026-07-09 已统一到 1.0.4）**：`site/api/version.json`、`site/api/version`、`project.yml`(macOS `MARKETING_VERSION`)、`xlsOne.xcodeproj/project.pbxproj`、`cpp/CMakeLists.txt`(`VERSION`) 全部统一为 `1.0.4`；`downloads.linux_amd64` 指向 `1.0.4`（需随本次重编的 linux-amd64 .deb 一并上传到 z-pulse.cn/downloads）。软著材料仍按申报版 V1.0.0 保留，不动。

---

## 五、最小可上线路径（MVP）

1. ~~国内后端 `api.z-pulse.cn` 部署 + 可达（含 `/offline` 与 `/downloads`）~~ ✅ **已完成**（2026-07-09 部署 + 签证书 + 自动续期）。
2. Windows 国内版编译（`package_windows_full.ps1 -Domestic` ✅ ZIP 已出）+ **代码签名**（⬜ 待采购证书 + signtool）+ 上传到 `api.z-pulse.cn/downloads`（⬜ 待 root 密码 / 上传）。
3. `version.json` / 下载页放开 Windows 真实链接（⬜ 待做）。
4. 爱发电回调联调，发码闭环（⬜ 待填 `AFDIAN_*`）。
5. （macOS）补国内 Developer ID 构建 + 签名 + 公证 + 上传，下载页放开 macOS。

> 第 2–4 完成即可在国内「能买、能激活、能下载、能安装」Windows 版（**当前以未签名方式分发**，SmartScreen 会弹未知发布者警告，用户点「仍要运行」即可；代码签名暂缓至盈利后采购）。macOS 国内直发在补完第 5 项后达成。
