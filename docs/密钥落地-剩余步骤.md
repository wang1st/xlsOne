# 密钥落地与客户端发布 — 剩余步骤（需你执行）

> 背景：Ed25519 密钥已于 2026-07-09 轮换，新种子 / ACTIVATION_SECRET 仅存在于本次会话，未入库、未提交。
> 客户端只有一把内嵌公钥，国内服务与 Cloudflare Worker 必须用**同一个新种子**，且两者都换完后再发布带新公钥的客户端。

## ✅ 已完成（2026-07-09，由助手 SSH 执行并验证）

- 国内 Node 服务 `47.97.115.235`：`.env` 的 `ED25519_PRIVATE_KEY` / `ACTIVATION_SECRET` 改为新值，删除旧缓存 `data/ed25519.pem`，`systemctl restart xlsone-activation`。
- **决定性验证**：服务端用新 PEM 签名，再用客户端公钥 `acc209fbd4e9e198c94aef7fc3faaf44f80e278357045959e1d6a2afb1044ceb` 验签 → 通过（sigLen 64）。服务 active、health ok。
- 注：服务器缺 `cryptography` 模块（CentOS 7 老 python，`pip` 无可用 wheel），PEM 由助手在本地（Python 3.13 + cryptography）生成后上传；运行时 Node 只读取 PEM，不需要 Python。建议把 `/opt/xlsone-activation/data/ed25519.pem` 纳入备份。

## ⏳ 待你执行 ①：Cloudflare Worker（沙箱无 wrangler 凭据）

```bash
cd activation/worker
wrangler secret put ED25519_PRIVATE_KEY --env production
# 提示输入时粘贴:
# 9ae69c76be9b3aa64d10e380bf68e47c98376bac8ecd452a478e5f615cb35274

wrangler secret put ACTIVATION_SECRET --env production
# 提示输入时粘贴:
# 8c6b78546cf47129e2eb020a078f7cc3dae5b8ce8b9d08289289bd4d334ff8c6

wrangler deploy --env production
```

⚠️ 必须与国内服务用**同一个新种子**，否则客户端（仅一把内嵌公钥）对任一方签发的授权都会验签失败。

## ⏳ 待你执行 ②：重编发布客户端（沙箱无构建环境 / 签名证书）

- 新公钥已在 `cpp/core/src/license_manager.cpp` 的 `kLicensePublicKey`，**无需再改**；建议先把 `license_manager.cpp` 与 `cpp/tests/core_tests.cpp` 的改动提交。
- **Windows 国内版**：用 `windows-cn-release` 预设构建（已将 `api.z-pulse.cn` 烤进客户端）→ `scripts/package_windows_msi_zip.ps1 -Domestic` 出 MSI/ZIP。
  - **代码签名（独立前置任务，当前脚本未做）**：需购买沃通 / TrustAsia 普通证书（或 EV 直过 SmartScreen），对 `.exe` / `.msi` 签名，否则 Windows SmartScreen 拦截。
- **macOS 国内直发**：在 `project.yml` 增 domestic scheme / plist，设 `XLSONEActivationBaseURL=https://api.z-pulse.cn`（App Store 渠道走国际、免授权，二者互斥，需先定渠道策略）→ Developer ID 构建 → `scripts/package_macos_swift_dmg.sh`（签名 + 公证）→ 上传。
- **上传与放开下载**：`python activation/domestic-server/deploy/upload_installer.py <xlsone-*.msi>`；并放开 `site/products/xlsone/download.html` 与 `site/api/version.json` 的 Windows / macOS 字段。

## 顺序约束

1. 完成 ①（Worker 同新种子）
2. 再发布 ②（带新公钥的客户端）

> 若有已用旧密钥签发的授权文件，换密钥后会被新客户端判为无效（产品尚"即将推出"，应无真实用户，可忽略）。
> 服务端 `ADMIN_API_KEY` 默认回退为 `ACTIVATION_SECRET`，改它也会改管理后台密钥——若用 admin API，记得用新值。
