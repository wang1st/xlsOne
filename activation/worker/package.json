{
  "name": "xlsone-activation-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "db:init": "wrangler d1 execute xlsone-license --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250408.0",
    "wrangler": "^3.114.0"
  }
}
